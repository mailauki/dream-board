export interface Tags { [key: string]: string | string[] | undefined }

export interface MetaTags {
	title: string,
	description: string,
	image: string,
	price: string,
	currency: string,
	baseURL: string,
}